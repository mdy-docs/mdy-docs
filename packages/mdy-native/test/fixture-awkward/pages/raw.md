Inline <b>bold</b> and <span class="x">a span</span> in a paragraph.

A tag split across <em>markdown *and* HTML</em> emphasis.

In a list:

- an item with <code>a tag</code> in it
- and one with a <br> that closes nothing

In a table cell:

| a |
| --- |
| <b>x</b> |

Inside `a code span <b>stays</b> literal`, and in an image alt:
![alt <b>text</b>](/i.png)

<div>a block, which already worked</div>

#!/usr/bin/env bash
set -euo pipefail
for f in "$@"; do
  if [[ -f "$f" ]]; then
    echo "reading ${f%.txt}" >&2
    count=$(wc -l < "$f")
  fi
done
function greet() { local name=${1:-world}; printf 'hello %s\n' "$name"; }
greet 'there' | tr a-z A-Z

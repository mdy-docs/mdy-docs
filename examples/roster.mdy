title: Team Roster
users:
  - { name: Ada Lovelace,  age: 36, lead: true  }
  - { name: Alan Turing,   age: 41, lead: false }
  - { name: Grace Hopper,  age: 45, lead: true  }
+++
= {{ res.data.title }}

% let leads = 0
% for (const u of res.data.users) { if (u.lead) leads++
- !!{{ u.name }}!! ({{ u.age }}){{ u.lead ? ' — team lead' : '' }}
% }

//{{ leads }} of {{ res.data.users.length }} are team leads.//

The raw config, shown as a normal code block (not consumed):

```yaml
title: Team Roster
```

Escaped interpolation renders literally: \{{ this stays as-is }}.

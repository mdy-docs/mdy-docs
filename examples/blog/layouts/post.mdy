= {{ req.title }}

//{{ req.date }}//

{{ req.content }}

% const related = req.related ?? []
% if (related.length > 0) {
!!More like this:!!

% for (const p of related) {
- [[ {{ p.title }} | {{ p.url }} ]]
% }
% }

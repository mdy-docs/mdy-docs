= {{ req.site.title }}

{{ req.site.description }}

% for (const p of req.posts) {
- [[ {{ p.title }} | {{ p.url }} ]] — {{ p.date }}
% }

% const page = req.pagination
% if (page.totalPages > 1) {
%   const newer = page.prevUrl ? "[[ ← newer | " + page.prevUrl + " ]] · " : ""
%   const older = page.nextUrl ? " · [[ older → | " + page.nextUrl + " ]]" : ""
{{ newer }}page {{ page.page }} of {{ page.totalPages }}{{ older }}
% }

# {{ arg.site.title }}

{{ arg.site.description }}

{% for (const p of arg.posts) { %}
- [{{ p.title }}]({{ p.url }}) — {{ p.date }}
{%
  }
  if (arg.pagination.totalPages > 1) {
%}

{% if (arg.pagination.prevUrl) { %}[← newer]({{ arg.pagination.prevUrl }}) · {% } %}page {{ arg.pagination.page }} of {{ arg.pagination.totalPages }}{% if (arg.pagination.nextUrl) { %} · [older →]({{ arg.pagination.nextUrl }}){% } %}
{% } %}

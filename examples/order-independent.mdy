# {{ self.report.title }}

Generated for **{{ self.report.owner }}**.

{% const total = self.items.reduce((s, i) => s + i.qty * i.price, 0) %}
| Item | Qty | Price | Line total |
| :--- | --: | --: | --: |
{% for (const i of self.items) { %}
| {{ i.name }} | {{ i.qty }} | ${{ i.price.toFixed(2) }} | ${{ (i.qty * i.price).toFixed(2) }} |
{% } %}

**Order total: ${{ total.toFixed(2) }}**

<!--
Data fences may appear anywhere — even below the template that uses them —
because every data fence is collected before the template runs.
-->

```data
report:
  title: "Invoice #42"
  owner: Grace Hopper
```

```data
items:
  - { name: Widget, qty: 3, price: 9.99  }
  - { name: Gadget, qty: 1, price: 24.50 }
```

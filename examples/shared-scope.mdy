start: 1
count: 5
+++
= Fibonacci

% let a = 0, b = res.data.start
% const out = []
% for (let i = 0; i < res.data.count; i++) { out.push(b); [a, b] = [b, a + b] }

The first {{ res.data.count }} numbers (starting at {{ res.data.start }}): !!{{ out.join(', ') }}!!.

% const sum = out.reduce((s, n) => s + n, 0)
Their sum is {{ sum }}.

start: 1
count: 5
+++
# Fibonacci

{% let a = 0, b = self.start %}
{% const out = [] %}
{% for (let i = 0; i < self.count; i++) { out.push(b); [a, b] = [b, a + b] } %}

The first {{ self.count }} numbers (starting at {{ self.start }}): **{{ out.join(', ') }}**.

{% let sum = out.reduce((s, n) => s + n, 0) %}
Their sum is {{ sum }}.
